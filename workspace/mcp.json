{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${WORKSPACE_DIR}"
      ],
      "disabled": true
    },
    "prts-vector": {
      "command": "prts-vector",
      "args": [
        "--db",
        "${WORKSPACE_DIR}/vector.db",
        "--dim",
        "1536"
      ],
      "disabled": true,
      "_note": "启用前需要 cargo build --bin prts-vector 并把 target/debug/prts-vector 加入 PATH，或写绝对路径"
    },
    "prts-workspace": {
      "command": "prts-workspace",
      "env": {
        "PRTS_WORKSPACE_DIR": "${WORKSPACE_DIR}"
      },
      "disabled": true
    }
  }
}
